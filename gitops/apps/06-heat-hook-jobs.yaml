---
# See gitops/manifests/heat-hook-jobs/job.yaml -- workaround for
# argoproj/argo-cd#23555. Wave 6, after heat's own Application
# (whatever wave that is) so its regular ConfigMaps/Secrets already
# exist for this Job's pods to mount.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: heat-hook-jobs
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "6"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:Speedylo/openstack_auto.git
    targetRevision: main
    path: gitops/manifests/heat-hook-jobs
  destination:
    server: https://kubernetes.default.svc
    namespace: openstack
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
